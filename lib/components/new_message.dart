import 'package:flutter/material.dart';

class NewMenssage extends StatelessWidget {
  const NewMenssage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Nova mensagem"),
    );
  }
}